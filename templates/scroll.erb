# ><%= "[ #{name} ]".center(75,'-') %><

@current_scroll = "<%= key %>"
@before_configs["<%= key %>"].call if @before_configs["<%= key %>"]
say_scroll '<%= name %>'

<% if config %>
<%= config.compile %>
<% else %>
config = {}
<% end %>

@configs[@current_scroll] = config

<%= template %>
